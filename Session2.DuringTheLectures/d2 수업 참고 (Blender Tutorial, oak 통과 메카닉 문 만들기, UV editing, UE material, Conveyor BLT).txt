alt + g, r, s = get back to the start point

Bevel 좌측하단 detail 창에서 각종 옵션 적용 가능
-custom에서 모양 조정 가능

두 엣지간 새 엣지 추가 = subdivide
같은 엣지간 분리 = subdivide
면에 등분 = subdivide

loop cut and slide = ctrl + r (오직 꼭지점이 4개면 싹다 썰음) <- 그래서 왠만하면 사각형으로 작업

<에디터 시작 세팅 변경>
file - defaults - save startup file - overwrite

bevel을 close 까지 했으면 mb ㄱㄱ

------

(edge mode) - alt left-click = 이어진 선 선택
ctrl + shift + '+' = 자동 선택
select 창 - select loops = 자동선택


shift + d = duplicate
transform pivot point(Snap 옆에 있음) - individual origins (가상 피봇 위치 수정)

--
---


<피벗 움직이기>
shift + 우클릭 = cursor 이동 / snap 킨 상태로 해야 깔끔
object mode 우클릭 - set orgin (origin = pivot)

<미러 이용해서 홈파기>
ctrl + shift + B = vertex bevel
그다음 edge에 bevel 해서 각을 자르고, 그걸 미러링

<메카닉 만들기>
K = knife , then a = angle then 15 후 click, enter when you've done

<머티리얼 적용하기>
assign해야 적용됨

alt + m = 오브젝트 내에서 split
p = 오브젝트로 부터 다른 오브젝트로서 seperate

<오브젝트 내에서 일부분 숨기기>
/는 오브젝트 숨기기용
ctrl + i(inverse) = 반전선택 후, h = hide
shift + h = 선택품 제외 hide

<vertex mode와 edge mode 같이 키기>
1 & shift + 2 

----------
---------

object를 export하고, 언리얼에 블렌더 오브젝트 import 하기

<언리얼 임포트시 체크 사항>
recompute normal / tangent의 체크들을 꺼야 각진 게 살아서 import 됨.
언리얼은 임포트시 모든 트랜스폼을 포기화 해버림, 피봇이 중요.

ctrl + spacebar = open drawer

<언리얼에서 머티리얼 개선하기 / 멋진 이펙트>
---
언리얼의 도량형
cm
초
0.0 ~ 1 (0% ~ 100%)
검정 ~ 흰
---
emissive = 발광도

빛이 매우 강하면 주변이 어둠게 보이는 착시

---

언리얼은 tick time by the Phases of 사인함수.
언리얼은 사인함수 주기로 시간 초를 셈
그러나 이를 타임노드와 사인 노드를 연결해, emissive에 multiply하여도,
0부터는 정보값이 없어서 -1까지 계속 검정만 보임.

If you do ABS on it, you'll get none of 0 ~ -1.0.
But the curve will have some stiff points

그래서 그냥 사인 함수 전체를 위로 올리고, 최댓값 이 넘지 않게,
더하고 나누어주면 됨.

----
----

<컨베이어 벨트 만들기>
object mode - 우클릭 - convert to (curve) = 점과 선만 있어 렌더링 불가 = geometry(점,선,면)
detail 창에서 data에서 geometry에서 extrude 가능

<전개도 피기>
Shift 우클릭으로 cursor 옮기고, mesh 하나 꺼내기
UV editing

(Texture paint로 색칠 가능)

-----

data - match texture space 하면 자동 적용
그다음 다시 메쉬로 convert 하려다가 수동으로 하네요. 쩔수

u = uv mapping
mark seam = uv 시 찢을 지점

다 선택 후 u, u
-------

전개도 용어 u, v, w = x, y, z = r, g, b

uv edit 에서 n, 전개도 vertex 선택 후 u&v 설정 가능

---

<언리얼에서 BLT 시뮬하기(gimmick or optimized)>

언리얼에서 리임포트

언리얼의 0,0 좌표는 데카르트 좌표계의 0,1 지점

ctrl +r = simul on off 

-----

face orientation 위에 wireframe 켜야 knife 선 보임

---




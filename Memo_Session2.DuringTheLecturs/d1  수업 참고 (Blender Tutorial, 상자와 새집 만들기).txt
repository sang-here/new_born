블렌더는 무료 프로그램

ai가 계속 발전해서, 아마 지금 금방이 3d관련 고점으로 추정 -> 프로젝트 두개 구성(현재: 디지털 트윈, 미래?: 본인 관심사)
소중한 시간 잘 사용해서, 나갈 때 원한다면 좋은 본인 작품 하나 만들어서 수료할 때 가지고 나가 시길. 

<튜토리얼>

image editor애서 원하는 사진 열기
(메뉴 해석 다 해보기가 사실 필요함)

숫자 패드 . = pin to the object = 언리얼 F 가까이 가기
Tab = 모드 변경
vertax = 점선, edge = 선, poly mesh face = 면 / 3D = 점선면
123키가 점선면 선택 키
(블렌더는 단축키로 다루어야 효율적)
G = grap / 우클릭 = 원위치
RGB = XYZ / vector 3

(영문 버전 써야 오류 없음)

n키 = show menu(transfrom)

Alt + G(grab), R(rotate), S(scale) 원위치

숫자패트: 1,3,7,9 정뷰 / 2,4,6,8 회전 입력

perspective view = 소실점, 원근뷰 / 숫자패드 5 othographic

-----
본디 작업 순서 = 형태 잡기 - bevel 등으로 vertex 추가, 근데 수업 상, 이번엔 vertex 먼저 해봄

<상자깍기>
edit mode
ctrl + B(bevel) + 휠 / +C = close (limit)
(ctrl + z or 우클릭 = 취소)

<엣지 merge하기>
a = all (전체선택)
m = merge / then b = by distance

<엣지들 분리하기>
alt = reset, reverse
alt + m = split

<face 늘리기>
s + x
scale 할 때는 마우스 띄어서 컨트롤 켜주면 미세 조정 쉬움
숫자패드 . 찍으면 다시 객체에게 aim

eidt - preference - system - undo step (ctrl + z history 늘리기)

<면에 두꼐 넣기>
object mode - 디테일창 - modifier(spanner icon) - add modifier - solidify - thickness up
offset - = 안으로, +는 밖으로

modifier를 edit mode일 때 끄기, edit mode 아이콘 끄면 됨

<썰어버리고, 굴곡 주기>
숫자패드 1 로 옆면 보고, 2번으로 edge mode
- edit mode 에서 w 하면 드래그 폼 변경
- shift + z = wire frame -> 뒤에 edge도 선택 가능
- 우클릭 - S, subdivide
(마우스 위치 영역에 따라 단축키 활성화 대상이 달라짐)

-굴곡
숫자 1번으로 vervex mode
shift + z 로 바닥 빼고 vertices 선택, propotional edit 켜서 스무스하게 g + x로 구기기

바닥만 드래그 선택 후, s + z로 점의 스케일 0만들어서 바닥 평평하게 만들기

<박스에 역 기울기 주기>
face mode - 사각형일 때만 alt 후 면의 edge를 클릭하면 그 줄 다 선택됨
soft selection = propotional edit mode / s + x,y 로 옆으로 늘려주기


<solidify 개선 및 수정하기>
tab - solidifier의 offset .5로 변경 (비율 조정)

a+a = 전체 선택 해제

edit mode - L (element) = 연결된 놈이 다 하나의 면 = 바닥면 전체 선택

상단 우측 창에서, show overlay - face orientation 클릭 
normal이 원래 반향 , flip = 반대 방향 
alt + n (normal) + f = flip / 빨간색으로 변하면 뒤집힌 flip 상태임

<삭제 후 반복 실습>
뷰포트에서 x + d는 삭제
-----
shift + a = add a thing (점,선,면이 있는게 mesh)
add 후 dimension 수정 (기본값은 너무 큼, 2m면) (늘 dimension과 scale을 미리 해두어야, 나중에 반복 작업이 없음)
ctrl + a = apply / scale 이 다 1이 됨

ctrl + B = bevel

(propostional edit이 켜지면 오류남 지정 면 scale 시)
우클릭 s = subdivide

rough하게 먼저 깎고 가 다음 vertices를 늘려야 함

s + z + 0 + enter = 짜부
face mode & alt + 우클릭 , s + y와 s + x로 기울기 부여

alt + n + f =flip

vertex를 늘리지 않으면서 smooth하게 만들기 = 우클릭, auto shade smooth (=음영 처리로 굴곡을 가려줌)

----
----
----

ctrl + n = new file

outliner - image editor - new - display mode - double click = 이미 불러오기

alt + g, r, s = back to the start point

(r은 특별히 x,y,z 안 해주면, 카메라 보는 방향으로 rotate)

gg, rr, ss = relative transform

object mode - ctrl + a = apply 기본값화 <- 여기서 부터 엉키면 처음부터 해야 함

--
--

dimensions를 알맞게 수정
(i = animatoion keyframe 추가, 움직이게 만들 때 씀)

(scale 조정은 edit mode에서 해야 apply한 게 안 무너짐)

(Bevel시 c하면, 안 넘어감)

a, then merge, by distance (겹친 vertex 병합)

(subdivide할 떄, edge mode로 해야 함.)

(너무 uniform 하면 인위적으로 보임.)
(3D에서는 빛과 그림자를 많이 생기게 해야 퀄리티가 좋아보임 = solidify의 offset을 그래서 약간 어긋나게 0.8로 설정)

object mode - shade auto smooth(각을 살리고 smooth / auto smooth 하면 각이 사라짐)

----
----
----

1. size 체크 - apply scale (ctrl + a)
2. 러프하게 작업해서 버텍스를 늘려서 작업
  error -> 버텍스나 edge가 겹쳐 있어서 기능이 작동을 안 한다.
             => Merge, By distance (m, b)
                 (그리고 vertices는 최소한이 좋음)

contents를 만들면 저작권

---

detail- scene - units - length Meters -> centimeters (언리얼과 동기화)
(모델링시 다른 것과 interaction을 꼭 고려하고 만들어야 함. Dimensions)
(지금이 모델링 산업 고점이니, 잘 배워유)

회사를 들어간다는 사고방식이 지체된 사고일지도 모름. 개인 역량과 팀과의 협업 능력이 필요
시키는 대로 배우지 말고, 능동적 사고가 필요해보임 = 개인 협상/창업/스타트업

apply = relative transform -> world transform

---

<새장 구멍 뚫기>

(모델링은 보통 사각형에서 시작함)
먼저 dimensions 맞추고, transform을 apply.

(기능 설명을 위해 work flow를 무시하고 하겠음)

ctrl + r, 왼 클릭(가르기), 우클릭(정가운데 가르기)
x, only edge&face = vertices만 살림

--

curdor의 기능 = anchor in UE
shift + a , add circle, radius(반지름 = 5)

(vertex 끼리 연결되어야 edge, edge to edge가 되어야 face, so connect the two vertices form the plane & the circle)
두 점 선택 후 f (=fill)
여러 edge들 선택 후 f -> 면 생성

<돌출 시키기>
face mode - e = Extrude
edge mode - shift + alt 누르고 edge - y눌러서 축 고정 후 - e = extrude, 그다음 바로 f 면채워서 뚜껑 닫음

<절반만 작업하기>
vertices 두개 잡고 j (=join) (이미 면이 있으니 f 대신 j)
x - f = face 삭제
add modifier - edit -mirror

<Bending 하기>
ctrl + r = bend 준비
문제 생기면 m, b 병합으로 vertex버리기
(vertices가 너무 많이 생김)
---
두 edge만 이용할 떈, 우클릭 s, 적절히 잡고, ctrl + B (bevel)

<분리하기>
alt m = y (split)
vertex 모드일 때, alt 누르면 사각형만 잡힘

<sanp 하기 shift + tab>
 선 하나 잡고, 상단 메뉴창 - snap - closest - vertex , g누르고 가져다 붙이기(x눌러서 수평이동으로만 고정해서 오류 잡기)

<object 분리하기 (seperate, p)>
p

<처마 만들기>
e로 Extrude하면 되는데, 면으로 만들어서, e 할거임
sePerate한 object에 solidify에서 ctrl a 하면 apply

<대들보 만들기>
shift + a = add 후 , g + y로 face 늘려주기

Face Orientation, proportional eiditing, snap
wireframe - shadeing viewport

